---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Home" description="Specialist suspension setup, Caterham engineering, and motorsport services from Simon Rogers. 30 years of championship-winning experience.">
  
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-content container">
      <div class="hero-text">
        <span class="hero-label">Suspension Engineering Excellence</span>
        <h1>Too Fast<br /><span class="text-green">To Race</span></h1>
        <p class="hero-subtitle">
          30 years of championship-winning suspension expertise. From custom suspension valving & engineering for all cars to complete Caterham builds, we engineer performance that wins.
        </p>
        <div class="hero-ctas">
          <a href="/contact" class="btn btn-primary">Get In Touch</a>
          <a href="/services" class="btn btn-outline">Our Services</a>
        </div>
      </div>
      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number">30+</span>
          <span class="stat-label">Years Experience</span>
        </div>
        <div class="stat">
          <span class="stat-number">1000+</span>
          <span class="stat-label">Dampers Built</span>
        </div>
        <div class="stat">
          <span class="stat-number">UK</span>
          <span class="stat-label">Based</span>
        </div>
      </div>
    </div>
    <div class="hero-scroll">
      <span>Scroll</span>
      <div class="scroll-line"></div>
    </div>
  </section>

  <!-- Services Overview -->
  <section class="services-overview">
    <div class="container">
      <div class="section-header">
        <span class="stripe-accent"></span>
        <h2>What We Do</h2>
        <p>Nothing is off the shelf – it's all engineered for you.</p>
      </div>
      
      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="3"/>
              <path d="M12 1v4M12 19v4M4.22 4.22l2.83 2.83M16.95 16.95l2.83 2.83M1 12h4M19 12h4M4.22 19.78l2.83-2.83M16.95 7.05l2.83-2.83"/>
            </svg>
          </div>
          <h3>Suspension Setup</h3>
          <p>Track, race, or road – we dial in your suspension to match your driving style and application. Data-driven setup using professional testing equipment.</p>
          <a href="/services#suspension" class="service-link">Learn More →</a>
        </div>
        
        <div class="service-card">
          <div class="service-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
            </svg>
          </div>
          <h3>Custom Valving</h3>
          <p>We're not all the same. Suspension and handling is subjective. Have your dampers valved specifically for YOU – your weight, your style, your preferences.</p>
          <a href="/services#valving" class="service-link">Learn More →</a>
        </div>
        
        <div class="service-card">
          <div class="service-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="2" y="7" width="20" height="14" rx="2" ry="2"/>
              <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/>
            </svg>
          </div>
          <h3>Chassis Engineering</h3>
          <p>Complete chassis setup and development. From geometry optimisation to corner weighting – we make your car work as a complete system.</p>
          <a href="/services#chassis" class="service-link">Learn More →</a>
        </div>
        
        <div class="service-card featured">
          <div class="service-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2L2 7l10 5 10-5-10-5z"/>
              <path d="M2 17l10 5 10-5"/>
              <path d="M2 12l10 5 10-5"/>
            </svg>
          </div>
          <h3>Caterham Specialist</h3>
          <p>Over 30 years owning, racing, and developing Caterhams. Bespoke builds, GSXR conversions, and complete engineering services for the Seven.</p>
          <a href="/caterham" class="service-link">Learn More →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Philosophy Section -->
  <section class="philosophy">
    <div class="container">
      <div class="philosophy-grid">
        <div class="philosophy-content">
          <span class="stripe-accent"></span>
          <h2>Our Philosophy</h2>
          <p class="lead">Communication. It has to be about communication.</p>
          <p>We listen to our customers' needs and develop a product or solution together. Whether you're chasing lap times or seeking the perfect road setup, we work with you to understand exactly what you need.</p>
          <p>With great IT and testing equipment, we're leading the way on service provision in the sector. Relaxed but professional – that's how we work.</p>
          <a href="/about" class="btn btn-outline">About Simon</a>
        </div>
        <div class="philosophy-image">
          <div class="image-frame">
            <img src="/images/IMG_3969.JPG" alt="Simon Rogers Engineering - @toofasttorace" />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Partners -->
  <section class="partners">
    <div class="container">
      <div class="section-header centered">
        <span class="stripe-accent"></span>
        <h2>Our Partners</h2>
        <p>Working with the best names in motorsport suspension.</p>
      </div>
      <div class="partners-logos">
        <div class="partner-logo">
          <span>Penske Racing Shocks</span>
        </div>
        <div class="partner-logo">
          <span>Nitron Racing Systems</span>
        </div>
        <div class="partner-logo">
          <span>Hyperco</span>
        </div>
        <div class="partner-logo">
          <span>Eibach</span>
        </div>
        <div class="partner-logo">
          <span>AiM Sports</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>Ready to Go Faster?</h2>
        <p>Get in touch to discuss your suspension setup, Caterham build, or engineering project.</p>
        <a href="/contact" class="btn btn-primary">Contact Us</a>
      </div>
    </div>
  </section>

</Layout>

<style>
  /* Hero Section */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 80px;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background: 
      linear-gradient(135deg, rgba(10, 10, 10, 0.95) 0%, rgba(10, 10, 10, 0.7) 100%),
      url('/images/Front Page Caterham Epynt GSXR.JPG') center/cover no-repeat;
    z-index: -1;
  }

  .hero-bg::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 200px;
    background: linear-gradient(to top, var(--tftr-black), transparent);
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: var(--space-xl);
    align-items: center;
  }

  .hero-label {
    display: inline-block;
    font-family: var(--font-heading);
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--tftr-green);
    margin-bottom: var(--space-sm);
    padding-left: 3rem;
    position: relative;
  }

  .hero-label::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    width: 2rem;
    height: 2px;
    background: var(--tftr-green);
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--tftr-grey-light);
    margin: var(--space-md) 0;
    max-width: 50ch;
  }

  .hero-ctas {
    display: flex;
    gap: var(--space-sm);
    margin-top: var(--space-md);
  }

  .hero-stats {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    padding-left: var(--space-lg);
    border-left: 1px solid rgba(92, 184, 92, 0.3);
  }

  .stat {
    display: flex;
    flex-direction: column;
  }

  .stat-number {
    font-family: var(--font-display);
    font-size: 3rem;
    color: var(--tftr-green);
    line-height: 1;
  }

  .stat-label {
    font-family: var(--font-heading);
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--tftr-grey);
  }

  .hero-scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: var(--tftr-grey);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
  }

  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, var(--tftr-green), transparent);
    animation: scrollPulse 2s infinite;
  }

  @keyframes scrollPulse {
    0%, 100% { opacity: 1; transform: scaleY(1); }
    50% { opacity: 0.5; transform: scaleY(0.8); }
  }

  /* Services Overview */
  .services-overview {
    background: var(--tftr-darker);
  }

  .section-header {
    margin-bottom: var(--space-lg);
  }

  .section-header.centered {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .section-header p {
    color: var(--tftr-grey-light);
    font-size: 1.1rem;
    margin-top: var(--space-xs);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-md);
  }

  .service-card {
    background: var(--tftr-dark);
    padding: var(--space-md);
    border: 1px solid rgba(255, 255, 255, 0.05);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 3px;
    height: 0;
    background: var(--tftr-green);
    transition: height 0.3s ease;
  }

  .service-card:hover {
    border-color: rgba(92, 184, 92, 0.3);
    transform: translateY(-4px);
  }

  .service-card:hover::before {
    height: 100%;
  }

  .service-card.featured {
    border-color: rgba(92, 184, 92, 0.3);
    background: linear-gradient(135deg, var(--tftr-dark), rgba(92, 184, 92, 0.05));
  }

  .service-icon {
    width: 50px;
    height: 50px;
    color: var(--tftr-green);
    margin-bottom: var(--space-sm);
  }

  .service-icon svg {
    width: 100%;
    height: 100%;
  }

  .service-card h3 {
    margin-bottom: var(--space-xs);
  }

  .service-card p {
    color: var(--tftr-grey-light);
    font-size: 0.95rem;
    margin-bottom: var(--space-sm);
  }

  .service-link {
    font-family: var(--font-heading);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.85rem;
    color: var(--tftr-green);
  }

  .service-link:hover {
    color: var(--tftr-green-light);
  }

  /* Philosophy */
  .philosophy {
    background: var(--tftr-black);
  }

  .philosophy-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
    align-items: center;
  }

  .philosophy-content .lead {
    font-size: 1.5rem;
    color: var(--tftr-white);
    margin-bottom: var(--space-sm);
  }

  .philosophy-content p {
    color: var(--tftr-grey-light);
    margin-bottom: var(--space-sm);
  }

  .philosophy-content .btn {
    margin-top: var(--space-md);
  }

  .image-frame {
    position: relative;
    aspect-ratio: 4/3;
    overflow: hidden;
  }

  .image-frame::before {
    content: '';
    position: absolute;
    inset: -10px;
    border: 2px dotted var(--tftr-green);
    transform: translate(20px, 20px);
    z-index: 1;
  }

  .image-frame img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: grayscale(20%);
  }

  /* Partners */
  .partners {
    background: var(--tftr-darker);
  }

  .partners-logos {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: var(--space-lg);
    margin-top: var(--space-md);
  }

  .partner-logo {
    padding: var(--space-md);
    background: var(--tftr-dark);
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .partner-logo span {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--tftr-grey-light);
  }

  /* CTA Section */
  .cta-section {
    background: linear-gradient(135deg, var(--tftr-green-dark), var(--tftr-green));
    text-align: center;
  }

  .cta-content h2 {
    color: var(--tftr-black);
  }

  .cta-content p {
    color: rgba(0, 0, 0, 0.7);
    font-size: 1.25rem;
    margin: var(--space-sm) auto var(--space-md);
    max-width: 50ch;
  }

  .cta-content .btn-primary {
    background: var(--tftr-black);
    color: var(--tftr-white);
  }

  .cta-content .btn-primary:hover {
    background: var(--tftr-darker);
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero-content {
      grid-template-columns: 1fr;
    }

    .hero-stats {
      flex-direction: row;
      border-left: none;
      border-top: 1px solid rgba(92, 184, 92, 0.3);
      padding-left: 0;
      padding-top: var(--space-md);
      margin-top: var(--space-md);
    }

    .philosophy-grid {
      grid-template-columns: 1fr;
    }

    .philosophy-image {
      order: -1;
    }

    .hero-ctas {
      flex-direction: column;
    }
  }
</style>
